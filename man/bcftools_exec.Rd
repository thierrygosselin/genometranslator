% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vcf.R
\name{bcftools_exec}
\alias{bcftools_exec}
\title{Run a bcftools command and log stderr}
\usage{
bcftools_exec(
  bcftools.path,
  args,
  log.file = NULL,
  label = NULL,
  verbose = TRUE,
  fail_on_status = TRUE
)
}
\arguments{
\item{bcftools.path}{(character) Path or name of the \code{bcftools} executable.}

\item{args}{(character) Vector of arguments passed to bcftools.}

\item{log.file}{(optional, character) Path to a log file. If not \code{NULL},
stderr is appended to this file with a small header.
Default: \code{log.file = NULL}.}

\item{label}{(optional, character) Short label used in the log header.
Default: \code{label = NULL}.}

\item{verbose}{(logical) If \code{TRUE}, the constructed command line is printed.
Default: \code{verbose = TRUE}.}

\item{fail_on_status}{(logical) If \code{TRUE} (default), a non-zero exit
status triggers an error. If \code{FALSE}, the status is returned invisibly.

Default: \code{fail_on_status = TRUE}.}
}
\value{
Invisibly returns a list with:
\itemize{
\item \code{status} – exit status (integer),
\item \code{stdout} – character scalar with STDOUT,
\item \code{stderr} – character scalar with STDERR.
}
}
\description{
Internal helper around \code{sys::exec_internal()} that:
\itemize{
\item runs \code{bcftools.path} with the supplied \code{args},
\item appends stderr to a log file (if provided),
\item optionally aborts on non-zero exit status.
}
}
\keyword{internal}
