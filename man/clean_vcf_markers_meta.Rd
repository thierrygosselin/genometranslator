% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vcf.R
\name{clean_vcf_markers_meta}
\alias{clean_vcf_markers_meta}
\title{title Clean and normalise markers metadata from a VCF import}
\usage{
clean_vcf_markers_meta(
  markers.meta,
  gds,
  data.source,
  ref.genome,
  stacks.checks = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{markers.meta}{A tibble/data.frame of markers metadata containing at
least \code{VARIANT_ID}, \code{CHROM}, \code{LOCUS}, \code{POS}.}

\item{gds}{A SeqArray GDS object with the VCF data already imported.}

\item{data.source}{(character) A short tag describing the originating
pipeline (e.g. \code{"Stacks"}, \code{"PLINK"}, \code{"ipyrad"},
\code{"freeBayes"}). May be updated inside the function (e.g. to
\code{"dart.vcf"} if the structure matches DArT VCF).}

\item{ref.genome}{(logical) Output of \code{detect_ref_genome()}, indicating
whether the data were produced with a reference genome.}

\item{stacks.checks}{(logical) Whether to apply additional Stacks-specific
adjustments when \code{ref.genome = FALSE}.
Default: \code{stacks.checks = FALSE}.}

\item{verbose}{(logical) Display messages during cleaning.
Default: \code{verbose = TRUE}.}
}
\value{
A list with:
\itemize{
\item \code{markers.meta}: cleaned tibble with harmonised fields;
\item \code{data.source}: (possibly updated) data source tag;
\item \code{detect.strand}: logical flag indicating whether strand info
was detected from the LOCUS field.
}
}
\description{
title Clean and normalise markers metadata from a VCF import
}
\keyword{internal}
