% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gds.R
\name{extract_markers_metadata}
\alias{extract_markers_metadata}
\title{extract_markers_metadata}
\usage{
extract_markers_metadata(
  gds,
  markers.meta.select = NULL,
  metadata.node = TRUE,
  whitelist = FALSE,
  blacklist = FALSE,
  verbose = FALSE
)
}
\arguments{
\item{gds}{The GDS object (genometranslator, legacy radiator, or SeqArray).}

\item{markers.meta.select}{(optional, character) Names of metadata fields to
import. For package GDS files, these are the column names in the metadata
node. For plain SeqArray GDS, standardized names
\code{VARIANT_ID}, \code{CHROM}, \code{LOCUS}, \code{POS} are mapped to
\code{variant.id}, \code{chromosome}, \code{annotation/id}, \code{position}.
Default: \code{markers.meta.select = NULL}.}

\item{metadata.node}{(logical) Whether to prefer the package metadata node
if present. Both \code{genometranslator} and legacy \code{radiator} nodes
are supported. If the node is missing or empty, the function falls back
to SeqArray nodes. Default: \code{metadata.node = TRUE}.}

\item{whitelist}{(logical) If \code{TRUE} and a \code{FILTERS} column is
present, only rows with \code{FILTERS == "whitelist"} are returned.
Default: \code{whitelist = FALSE}.}

\item{blacklist}{(logical) If \code{TRUE} and a \code{FILTERS} column is
present, only rows with \code{FILTERS != "whitelist"} are returned.
Default: \code{blacklist = FALSE}.}

\item{verbose}{Logical indicating whether progress messages are emitted.
Default: \code{verbose = FALSE}.}
}
\description{
Import markers metadata from a genometranslator or SeqArray GDS.
}
