% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write_kinference.R
\name{write_kinference}
\alias{write_kinference}
\title{Write kinference}
\usage{
write_kinference(
  data,
  strata = NULL,
  filename = NULL,
  path.folder = getwd(),
  chunk.size = 32L,
  overwrite = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{data}{Open SeqArray GDS or path to a GDS file. Active whitelists apply.}

\item{strata}{Optional sample metadata table or tab-separated file containing
unique INDIVIDUALS. All active samples must be present. Columns are retained;
rows are matched by ID, not position. Defaults to the GDS sample metadata.}

\item{filename}{Optional output basename, without the .rds extension.}

\item{path.folder}{Output directory.}

\item{chunk.size}{Number of samples per GDS read block.}

\item{overwrite}{Allow replacement of existing export files.}

\item{verbose}{Display progress messages, wrapped to 80 characters.}
}
\value{
Invisibly, a gbasics snpgeno object. The export.files attribute lists
the RDS and sample, locus and allele mapping tables. The RDS contains the
sample metadata and locus/allele correspondence needed to interpret results.
}
\description{
Convert complete diploid biallelic SNP calls from a SeqArray
GDS to a gbasics snpgeno object and save it as RDS for kinference.
This is a format conversion, not genotype calling or kinship inference.
}
\details{
Requires the optional package gbasics (>= 1.2). Install it from
https://markbravington.r-universe.dev; it is not installed automatically.
Uses its public snpgeno constructor, not a manually assembled raw object.

REF is A and ALT is B (not necessarily major and minor alleles). Calls 0/0,
0/1 and 1/1 become AAO, AB and BBO in the standard four-way encoding.
Here AAO and BBO denote observed homozygotes under the no-null model: the
exporter does not infer null alleles. Use kinference::est_ALF_nonulls() to
estimate frequencies before kin_power(); est_ALF_ABO_quick() would instead
fit a heritable-null model and is not the default biological interpretation.

Missing and partially missing calls are rejected, never recoded to OO.
OO denotes a biological double-null, not inadequate depth or failed calling.
Indels, symbolic alleles, multiallelic records, invariant selected loci and
presence/absence observations are rejected. Filter explicitly beforehand.
No automatic thinning, imputation, frequency estimation or population pooling
decision is performed. Retain one genetically homogeneous population and
assess batch effects, HWE, genotyping error and LD before kin-finding.

The output is in memory, not file-backed. Block reads limit temporary GDS
extraction, but the full conversion must fit in RAM. GDS selections are
restored on success or failure; the original GDS is not changed. Depth,
likelihoods and phase are not exported. Keep the GDS for those quantities.

In kinference 1.2.48, pair-finding routines can lose character pair
identifiers or fail. Use ij_numeric = TRUE and map returned indices through the
exported object's info$INDIVIDUALS until that upstream issue is resolved.
}
\examples{
\dontrun{
x <- genometranslator::write_kinference(
  "study.gds", strata = "samples.tsv", filename = "study",
  path.folder = "exports"
)
# After population and genotype QC:
x <- kinference::est_ALF_nonulls(x)
x <- kinference::kin_power(x, k = 0.5)
}
}
\references{
Bravington MV, Baylis SM, Eveson JP, Feutry P (2026).
kinference: Pairwise kinship detection for Close-Kin Mark-Recapture.
bioRxiv preprint. \doi{10.64898/2026.05.18.725841}.
}
\seealso{
https://closekin.r-universe.dev/kinference
}
\author{
Thierry Gosselin \email{thierry.gosselin@csiro.au}
}
