# strata_haplo

Manage strata

## Usage

``` r
strata_haplo(strata = NULL, data = NULL, blacklist.id = NULL)
```

## Arguments

- strata:

  Default: `strata = NULL`.

- data:

  Default: `data = NULL`.

- blacklist.id:

  Default: `blacklist.id = NULL`.
