# checks_dart_target

Checks target.id and strata for DArT potential problems.

## Usage

``` r
checks_dart_target(target.id, strata, path.folder = NULL, verbose = TRUE)
```

## Arguments

- path.folder:

  Default: `path.folder = NULL`.

- verbose:

  Default: `verbose = TRUE`.
