# dart_genotyping_strategy

Generating genotypes coding is computer costly, strategy ...

## Usage

``` r
dart_genotyping_strategy(
  n.markers,
  gt.vcf = NULL,
  gt.vcf.nuc = NULL,
  gt = NULL
)
```

## Arguments

- gt.vcf:

  Default: `gt.vcf = NULL`.

- gt.vcf.nuc:

  Default: `gt.vcf.nuc = NULL`.

- gt:

  Default: `gt = NULL`.
