# switch_allele_count

Switch from dart genotyping 2 presence absence

## Usage

``` r
switch_allele_count(x, dart.check = FALSE, ref = TRUE)
```

## Arguments

- dart.check:

  Default: `dart.check = FALSE`.

- ref:

  Default: `ref = TRUE`.
