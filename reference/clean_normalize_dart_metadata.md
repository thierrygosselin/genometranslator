# clean_normalize_dart_metadata

clean_normalize_dart_metadata (only the DArT columns = snakecase...)

## Usage

``` r
clean_normalize_dart_metadata(
  x,
  dart.check,
  blacklist.id = NULL,
  strata = NULL
)
```

## Arguments

- blacklist.id:

  Default: `blacklist.id = NULL`.

- strata:

  Default: `strata = NULL`.
